

To use:

Example:

Map map = new Map(100, 120, 1000);

where 100 is x size of map, 120 is y size, and 1000 is number of steps to generate the map, analogous to size and complexity.

map.IntMap will then give List<List<int>> of the map, where (1) is a wall and (0) is open space.